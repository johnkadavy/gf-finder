// Returns null — custom font loading disabled until a compatible TTF source is available.
// OG images fall back to system sans-serif.
export async function loadBebasNeue(): Promise<ArrayBuffer | null> {
  return null;
}
